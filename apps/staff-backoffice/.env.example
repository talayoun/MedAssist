# API base URL. Staff Back-Office is desktop-only (per constitution) -- keep this
# on localhost, never a LAN IP, or the session cookie (SameSite=Lax) won't survive
# cross-site requests from a localhost-served page.
VITE_STAFF_API_URL=http://localhost:3000
